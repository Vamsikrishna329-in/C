// Conversation of F to C
#include <stdio.h>

int main() {
    float temperature;
    printf("Enter the temperature in Fahrenheit :");
    scanf("%f",&temperature);
    float f = (temperature - 32) / 1.8;
    printf("The temperature in Celsius  is: %.2f\n",f);
    return 0;
}
